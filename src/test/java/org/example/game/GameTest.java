package org.example.game;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class GameTest {
    @Test
    void wordContainsLetter() {
        String word = "abc";
        Game game = new Game(new char[]{}, new char[]{}, 0);
        assertTrue(true);
    }
}
